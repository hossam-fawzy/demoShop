<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="DemoWebShop Test Suite" parallel="tests" thread-count="2" verbose="1">

    <!-- Listeners for better reporting -->
    <listeners>
        <listener class-name="io.qameta.allure.testng.AllureTestNg"/>
    </listeners>

    <!-- Smoke Test Suite -->
    <test name="Smoke Tests" preserve-order="true">
        <classes>
            <class name="tests.LoginTest"/>
            <class name="tests.HomeTest"/>
        </classes>
    </test>

    <!-- Regression Test Suite (commented out for quick runs) -->
    <!--
    <test name="Regression Tests">
        <classes>
            <class name="tests.LoginTest"/>
            <class name="tests.HomeTest"/>
            <class name="tests.RegistrationTest"/>
            <class name="tests.CheckoutTest"/>
        </classes>
    </test>
    -->

    <!-- Test by Groups (example) -->
    <!--
    <test name="Critical Tests">
        <groups>
            <run>
                <include name="smoke"/>
                <include name="critical"/>
            </run>
        </groups>
        <packages>
            <package name="tests.*"/>
        </packages>
    </test>
    -->

</suite>